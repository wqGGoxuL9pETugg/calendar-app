export type ScheduleItem = {
  id: string
  title: string
  notes: string
  dateKey: string
  sourceInput: string
  createdAt: string
  updatedAt: string
}

export type ScheduleInput = {
  title: string
  notes: string
  dateKey: string
  sourceInput: string
}
